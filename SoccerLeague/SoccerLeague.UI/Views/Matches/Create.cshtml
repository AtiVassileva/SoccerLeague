@using SoccerLeague.UI.Infrastructure
@model MatchRequestModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Match</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="LeagueId" class="control-label"></label>
                <select asp-for="LeagueId" class="form-control">
                    @foreach (var kvp in ViewBag.Leagues)
                    {
                        <option value="@kvp.Key">@kvp.Value</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="HostId" class="control-label"></label>
                <select asp-for="HostId" class="form-control">
                    @foreach (var kvp in ViewBag.Teams)
                    {
                        <option value="@kvp.Key">@kvp.Value</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="GuestId" class="control-label"></label>
                <select asp-for="GuestId" class="form-control">
                    @foreach (var kvp in ViewBag.Teams)
                    {
                        <option value="@kvp.Key">@kvp.Value</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="ArenaId" class="control-label"></label>
                <select asp-for="ArenaId" class="form-control">
                    @foreach (var kvp in ViewBag.Arenas)
                    {
                        <option value="@kvp.Key">@kvp.Value</option>
                    }
                </select>
            </div>
            <div class="form-group">
                <label asp-for="PlayedOn" class="control-label"></label>
                <input asp-for="PlayedOn" class="form-control" />
                <span asp-validation-for="PlayedOn" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HostGoals" class="control-label"></label>
                <input asp-for="HostGoals" class="form-control" />
                <span asp-validation-for="HostGoals" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="GuestGoals" class="control-label"></label>
                <input asp-for="GuestGoals" class="form-control" />
                <span asp-validation-for="GuestGoals" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Country" class="control-label"></label>
                <input asp-for="Country" class="form-control" />
                <span asp-validation-for="Country" class="text-danger"></span>
            </div>
            <div class="form-group visually-hidden">
                <label asp-for="AuthorId" class="control-label"></label>
                <input asp-for="AuthorId" class ="form-control" value="@User.GetId()"/>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-info" asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
